services:
  crewai-env:
    build: .
    container_name: sbir-crewai-templatenumber
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    env_file:
      - .env  # Or .env.local
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - SERPER_API_KEY=${SERPER_API_KEY}